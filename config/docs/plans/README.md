# Plans

`/plan` writes one file per task here, named `YYYY-MM-DD-short-slug.md`, with a `Status:` line at the top (open or closed), the acceptance criteria, and the steps. Commits on the task reference the plan file. Closed plans stay; they are the record of what was intended versus what shipped.
