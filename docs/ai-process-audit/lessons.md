# Lessons

Corrections turned into controls. `/lesson` appends a row here. If the same mistake appears twice, the control did not work; pick a stronger one.

| date | what went wrong | root cause | control added | where |
| --- | --- | --- | --- | --- |
| 2026-09-13 | The first real install (SEP-Quoting, another cloud session) could not use the documented `curl \| bash` one-liner: the GitHub archive host returned 403 through that session's proxy. The one-liner had worked from a different session, so the installer relied on something not reliable across cloud sessions | missing validation (the install path was only ever tested from a session whose proxy allowed the archive) | Installer falls back to `git clone` when the archive download fails, proven against the live proxy; trap recorded in `beyond-traps`; cloud install docs now lead with the clone-based path | `install.sh`, `config/.claude/skills/beyond-traps/SKILL.md`, `docs/INSTALL.md` |
| 2026-09-13 | The same install ran from a main that predated the installer's keep-a-project's-own-command fix, so the target's custom `.claude/commands/review.md` was deleted rather than kept | missing feedback loop (the fix existed but the install predated it) | Confirmed the fix (PR #8) is the right control; the driving session preserved the checklist's substance in the Project section by hand, which is the better outcome and is now the documented pattern in `docs/PROJECT-SECTION.md` | `install.sh` (already fixed), `docs/PROJECT-SECTION.md` |
| 2026-09-11 | Installer re-run overwrote the filled-in Project section of CLAUDE.md while the docs claimed it was preserved | missing validation | End-to-end re-run test that appends to the Project section and asserts it survives | `scripts/verify.sh` |
| 2026-09-11 | verify.sh built its fixture tarball from tracked files only, so untracked new files were "missing" from the install | missing validation | Tarball built from tracked plus untracked files | `scripts/verify.sh` |
| 2026-09-11 | `node --test tests/` on Node 22 treats the directory as a test file and reports one failure | missing context | verify.sh is the only documented way to run tests and uses an explicit glob | `scripts/verify.sh`, `CONTRIBUTING.md` |
| 2026-09-11 | The em dash check in CONTRIBUTING.md contained a literal em dash and flagged itself | poor instruction | Byte-escaped pattern in the scan; docs no longer carry the literal | `scripts/verify.sh` |
| 2026-09-11 | Tried to rename the repo through the GitHub API after a probe returned 200; the cloud proxy refuses repository-settings writes | missing tool | Recorded as a known environment limit so no session tries again | `CLAUDE.md` Project section, `docs/decisions.md` |
| 2026-09-11 | A 22-file change was built with no plan on disk and no review pass, in the repo that mandates both | missing feedback loop | The repo now installs its own payload, so `/plan`, `/review` and `/lesson` are available here and verify.sh fails if the root copy drifts | `scripts/sync-root.sh`, `scripts/verify.sh` |
| 2026-09-12 | Shell guard treated a bare `env` as a wrapper with nothing after it and let it through | missing validation | Test case for bare `env`; wrapper stripping requires a following command | `tests/bash-guard.test.js` |
| 2026-09-12 | Tried to fetch third-party skill archives through api.github.com and github.com; the session proxy serves only attached repos that way. Public repos are cloneable with plain git through the proxy's anonymous read lane | missing context | `scripts/vendor-skills.sh` uses git clone at a pinned commit; the lesson is in `docs/decisions.md` | `scripts/vendor-skills.sh` |
| 2026-09-12 | Tried to push the release tag, then to create it through the API; the proxy refused both, and the first attempt's error was hidden by a `tail -1` so the pinned-install test ran against a tag that did not exist | missing tool, then missing validation | Environment limit recorded in `CLAUDE.md` and `docs/decisions.md`; release tags are the owner's step in `CONTRIBUTING.md` | `CONTRIBUTING.md`, `docs/decisions.md` |
| 2026-09-12 | Passed a merge call a head SHA reconstructed from its short form; GitHub refused with a 409 | poor instruction (the never-invent rule applied to me) | Read the full SHA from git before any call that pins one | `docs/ai-process-audit/lessons.md` |
| 2026-09-12 | Shell guard blocked a documentation command because a backtick pair inside a heredoc looked like command substitution. Found the moment the root install made the guard live in this session | missing validation | Heredoc bodies are treated as data unless piped to a shell or SQL client; six test cases | `config/.claude/hooks/bash-guard.js`, `tests/bash-guard.test.js` |
